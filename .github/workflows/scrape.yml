name: Scrape Flashscore mobi (fast-safe)

on:
  workflow_dispatch:
  schedule:
    - cron: "30 4 * * *"  # ~07:30 Europe/Bucharest

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"   # fără cache:"npm" — nu avem lockfile

      # Cache pentru ~/.npm (nu are nevoie de lockfile)
      - name: Cache npm dir
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-cheerio-1.0.0-rc.12
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Install cheerio
        run: npm install cheerio@^1.0.0-rc.12 --no-fund --no-audit --prefer-offline

      - name: Run scraper (football, d=1)
        timeout-minutes: 5
        env:
          DAY_OFFSET: "0"             # 0=today, 1=tomorrow
          MAX_MATCHES: "36"
          CONCURRENCY: "6"
          FETCH_TIMEOUT_MS: "12000"
          RETRIES: "1"
          GLOBAL_HARD_STOP_MS: "150000"
        run: node scrape_mobi.js

      # 🆕 generează biletele din odds.json
      - name: Generate tickets
        run: npm run tickets
        env:
          COTA2_TOL: "0.15"   # ±15% toleranță pentru Cota 2
          ZI_TOL: "0.30"      # ±30% toleranță pentru Biletul Zilei

      # urcă toate fișierele rezultate
      - name: Upload outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: output
          path: |
            list.html
            match-*.html
            matches.json
            odds.json
            tickets.json
            tickets.md
