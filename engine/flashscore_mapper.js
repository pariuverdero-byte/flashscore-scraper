/**
 * Fallback mapping using Flashscore
 * This will be implemented later using API-Football / Flashscore scraper
 */
export async function flashscoreMapMatch({ sport, rawText }) {
  // TODO:
  // 1. detect league / competition
  // 2. search in Flashscore live / upcoming
  // 3. fuzzy match teams / players

  return null;
}
